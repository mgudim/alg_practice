#ifndef BALANCED_BRACKETS
#define BALANCED_BRACKETS
#include <stdbool.h>
#include <stddef.h>


//Given the string str consisting of characters '(' and '(' return true
//if the string is a balanced bracket expession and false otherwise.
bool areBracketsBalanced(const char* str, size_t numChars);

#endif
