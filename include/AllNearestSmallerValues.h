#ifndef ALL_NEAREST_SMALLER_VALUES
#define ALL_NEAREST_SMALLER_VALUES
#include <stddef.h>


//Given array of integers arr, for each value x in arr find the index of smallest value to the left of a in arr.
void findAllNearestSmallerValues(
    int* arr,
    size_t numElements,
    int* res
);


#endif
