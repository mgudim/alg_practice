#ifndef RAIN_WATER
#define RAIN_WATER
#include <stddef.h>
//
//
//Given array of integers arr, for each value x in arr find the index of smallest value to the left of a in arr.
unsigned findWaterTrapped(unsigned* heights, size_t size);


#endif
